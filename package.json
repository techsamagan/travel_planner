{
  "name": "wanderwise",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Wanderwise — AI Travel Planner (Vercel monorepo: static client + serverless API)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev:server": "node --watch server/server.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "openai": "^4.104.0",
    "pg": "^8.13.1"
  }
}
